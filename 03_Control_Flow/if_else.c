#include <stdio.h>

int main() {
    int x = 10;

    if (x <= 9) {
        printf("result 1 ( x <= 9 ) : %d\n", x);
    }
    else {
        printf("result 2 ( x > 9 ) : %d\n", x);
    }

    return 0;
}